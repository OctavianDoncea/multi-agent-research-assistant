# Leave empty for local Vite (uses /api proxy to localhost:8000).
# On Vercel, set to your Render backend origin (no trailing slash):
# VITE_API_BASE_URL=https://your-backend.onrender.com
VITE_API_BASE_URL=
